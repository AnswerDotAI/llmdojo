Python project detected. Imports are loaded, including rg/fd/ls from rgapi. If the doc() output for these modules isn't visible in your context, read it now, by calling these in separate cells:
    doc(clik, pysk, edsk)
    doc(dsk, exh, rgsk)
    doc(acp)
    list_pyskills()
